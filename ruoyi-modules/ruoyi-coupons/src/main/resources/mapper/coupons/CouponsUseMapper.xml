<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.coupons.mapper.CouponsUseMapper">
    <insert id="associateUserWithCoupon">
        insert into coupons_use
        set user_id=#{userId}, coupon_id =#{couponId}, receive_time=now(), is_used=0
    </insert>


    <select id="getReceivedCountForUser" resultType="java.lang.Integer">
        SELECT COUNT(*) AS receivedCount
        FROM coupons_use
        WHERE user_id = #{userId}
          AND coupon_id = #{couponId}
    </select>
</mapper>